# Machine-specific settings for this computer. This file is git-ignored - never commit it.
# Replace CHANGE_ME with the password of the MySQL account below (and change the username if needed).

spring.datasource.username=root
spring.datasource.password=root
